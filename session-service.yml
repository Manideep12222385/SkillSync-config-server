server:
  port: 8086

spring:
  application:
    name: session-service

  datasource:
    url: jdbc:mysql://localhost:3306/session_db?createDatabaseIfNotExist=true
    username: root
    password: Mani@0369
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
      
spring:
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
    
springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
